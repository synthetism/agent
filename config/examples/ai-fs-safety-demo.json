{
  "name": "AI-FS Safety Demonstration Agent",
  "description": "Demonstrates safe AI filesystem operations with sandbox constraints",
  "version": "1.0.0",
  "identity": {
    "systemPrompt": "You are an AI filesystem safety demonstration agent. You operate within strict safety constraints and demonstrate secure autonomous file operations. Always verify operation safety before execution.",
    "promptTemplate": "Task: {{task}}. Use tool: {{tool}} safely within constraints. Goal: {{goal}}. Context: {{context}}",
    "missionStrategy": {
      "analysis": "Analyze filesystem tasks for safety compliance",
      "toolSelection": "Use only approved filesystem operations within sandbox",
      "promptGeneration": "Generate constrained prompts with safety validation",
      "responseEvaluation": "Verify all operations stay within safety boundaries"
    }
  },
  "safetyConstraints": {
    "description": "Strict filesystem safety rules for AI-FS demo",
    "allowedOperations": [
      "read_file",
      "create_file", 
      "replace_string_in_file",
      "list_dir",
      "file_search"
    ],
    "restrictedOperations": [
      "run_in_terminal",
      "system_commands",
      "network_operations",
      "delete_file",
      "move_file"
    ],
    "pathConstraints": {
      "allowedPaths": [
        "./demo/**",
        "./temp/**", 
        "./sandbox/**"
      ],
      "restrictedPaths": [
        "./src/**",
        "./config/**",
        "./node_modules/**",
        "../**",
        "/system/**",
        "/usr/**",
        "/etc/**"
      ]
    },
    "operationLimits": {
      "maxFileSize": "1MB",
      "maxOperations": 20,
      "maxDepth": 3,
      "timeoutSeconds": 300
    }
  },
  "demoScenarios": [
    {
      "name": "Safe File Creation",
      "description": "Create files within sandbox with safety validation",
      "task": "Create a weather report file in the demo folder",
      "expectedTools": ["create_file"],
      "expectedPaths": ["./demo/weather-report.md"],
      "safetyChecks": ["path validation", "operation approval", "size limits"]
    },
    {
      "name": "Constrained File Reading", 
      "description": "Read files only from allowed directories",
      "task": "Read and summarize files from the demo directory",
      "expectedTools": ["list_dir", "read_file"],
      "expectedPaths": ["./demo/"],
      "safetyChecks": ["path restrictions", "directory traversal prevention"]
    },
    {
      "name": "Safe Content Modification",
      "description": "Modify file contents with validation",
      "task": "Update a configuration file with new settings",
      "expectedTools": ["read_file", "replace_string_in_file"],
      "expectedPaths": ["./demo/config.json"],
      "safetyChecks": ["backup creation", "content validation", "rollback capability"]
    }
  ],
  "validation": {
    "pathSafetyCheck": true,
    "operationWhitelist": true,
    "sizeConstraints": true,
    "depthLimiting": true,
    "timeoutProtection": true
  },
  "monitoring": {
    "logAllOperations": true,
    "trackViolations": true,
    "generateSafetyReports": true,
    "alertOnConstraintViolation": true
  },
  "metadata": {
    "purpose": "AI-FS safety demonstration for enterprise adoption",
    "targetAudience": "DevOps teams, security engineers, AI platform developers",
    "demonstratedConcepts": [
      "Sandboxed AI operations",
      "Path constraint enforcement", 
      "Operation whitelisting",
      "Safety validation layers",
      "Autonomous oversight"
    ]
  }
}
